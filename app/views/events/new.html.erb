<% provide(:title, 'New Event') %>
<%= render "application/header" %>
<%= form_for(@event) do |f| %>
 
  <%= f.label :name %>
  <%= f.text_field :name %><br>

  <%= f.label :datetime %>
  <%= f.date_field :datetime %><br>

  <%= f.submit %>

<% end %>

